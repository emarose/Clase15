// Crear una variable que filtre los animales que no sean 'cat' 
// usar filter

const pets = ['cat', 'dog', 'elephant','hawk','snake','cat']

let result = pets.filter(v => v !== 'cat');
console.log(result);
